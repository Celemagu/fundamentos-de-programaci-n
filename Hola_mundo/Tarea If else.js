/**
 * 
 */

 let note = 80;
 
 if (note >=90) {
	 console.log ("La nota del alumno es : A con una calificacion de " + note);
 } else if (note >=80){
	 console.log ("La nota del alumno es : B con una calificacion de " + note);
 }else if (note >=70){
	 console.log ("La nota del alumno es : C con una calificacion de " + note);
 }else if (note >=60){
	 console.log ("La nota del alumno es : D con una calificacion de " + note);
 }else {
	 console.log ("La nota del alumno es : F con una calificacion de " + note);
 }